# 작업 로그

작업이 끝날 때마다 여기에 기록합니다. **최신 항목이 위**로 옵니다.

기록의 목적은 두 가지입니다. 첫째, 다음에 이어받는 사람(또는 미래의 나)이 맥락을 복원할 수 있게 합니다. 둘째, [`growth-roadmap.md`](./growth-roadmap.md)의 "배운 점"이 실제로 쌓이게 합니다. 특히 **측정한 수치는 그때 적지 않으면 재현이 어렵습니다.**

## 기록 템플릿

```markdown
## YYYY-MM-DD — 제목

- **마일스톤**: M0
- **브랜치**: claude/...

### 한 일
- 

### 검증
- 어떻게 확인했는가 (명령어, 실측 수치, 실기기 확인 등)

### 배운 점
- 자기 검증 질문 중 답하지 못한 것
- 측정한 수치
- 막혔던 지점과 실제 원인
- 다시 한다면 다르게 했을 선택

### 다음 할 일
- 
```

---

## 2026-07-25 — 디자인 에셋 패키지 + 사용 가이드

- **마일스톤**: M0 착수 전 (사전 정비)
- **브랜치**: `claude/game-graphics-design-files-zzp2br`

### 한 일

- **코드보다 먼저 디자인 레이어를 만들었다.** `design/` 에 SVG 40개 + 토큰 CSS 1개 + 프리뷰 페이지 1개.
  - `tokens/tokens.css` — 2계층 토큰(원시 `--c-*` → 시맨틱 `--color-*`). 색·간격·타이포·z-index·모션. M0에서 `app/globals.css` 로 이식할 수 있게 `:root` 블록 하나로 작성.
  - `character/jupsy-base.svg` — 확정된 디자인 언어대로 매끈한 흰 셸, 분리되어 떠 있는 머리와 팔, 고양이 귀 센서, 등 가시, 수염 와이어, 가슴 코어 라이트.
  - `character/expressions/` 8종 — 눈 슬롯 `120×60`, 캐릭터 캔버스의 `translate(40 52)` 자리에 드롭인 교체.
  - `arcade/` — 쓰레기 4종, 위험 파편 2종, 배경 3레이어, 이펙트 3종.
  - `ui/` — 아이콘 8종, 배지 빈 틀, 궤도 정화율 시각화, 로고·파비콘·워드마크.
  - `preview.html` — 의존성 없는 단일 HTML. 앱이 없는 지금도 에셋을 눈으로 검증할 수 있다.
- `docs/design-guide.md` 를 썼다 — 파일 구조·네이밍, 토큰 이식 절차, SVG 작성 규칙, 표정 교체 규격, 애니메이션 규약, 성능, 접근성, 마일스톤 대응표.
- ADR-008을 추가했다 (디자인 에셋을 코드보다 먼저, 레포 안에서, 빌드 없이).
- **미확정 항목을 디자인이 먼저 결정하지 않도록 틀만 만들었다.** 배지는 빈 프레임만(Q3 미결), 성격 유형 4종은 몸체를 복사하지 않은 액센트 레이어 **제안**으로만(M6 미결).

### 검증

- SVG 40개 전부 XML well-formed (`xml.dom.minidom.parse`)
- `viewBox` 누락 0건 / 루트에 `width`·`height` 하드코딩 0건
- 색 리터럴이 `var(--토큰, #폴백)` 바깥에 남은 곳 0건 (팔레트 참조 시트 제외)
- Chromium 헤드리스로 `preview.html` 렌더 후 스크린샷 육안 확인 — 캐릭터, 표정 8종, 유형 제안 4종, 쓰레기·위험·이펙트·배경, 아이콘·배지·궤도·로고 전부 정상
- 표정 교체가 실제로 바이저에 맞아떨어지는지 프리뷰에서 확인 (몸통 SVG는 그대로)
- 대비비 계산 — 텍스트·강조 토큰 전부 WCAG AA(4.5:1) 통과. 가장 빠듯한 것은 `--color-hazard` 4.5:1(surface-raised 위)
- 문서 상대 링크가 실제 파일과 일치하는지 확인

### 배운 점

- **XML 주석 안에서는 `--` 가 불법이다.** 토큰 이름을 주석에 그대로 적었다가 SVG 5개가 통째로 파싱 실패했다(`orbit-band`, `absorb-burst`, `type-archive`, `palette`, `badge-frame`). 브라우저는 관대해서 그려주기 때문에 눈으로는 안 잡히고, XML 파서를 돌려야 나온다. 주석에는 접두사를 빼고 `color-eye 토큰` 처럼 쓰기로 규칙을 정했다.
- **`<img src="x.svg">` 로 넣으면 CSS 변수가 SVG 안으로 상속되지 않는다.** 그래서 모든 색을 `var(--토큰, #폴백)` 로 썼다. 덕분에 `preview.html` 이 제대로 보이는 것 자체가 "폴백이 전부 살아 있다"는 검증이 된다. 앱에서는 인라인으로 넣어 토큰이 적용된다.
- **거대한 원의 일부만 화면에 보일 때 `objectBoundingBox` 그라디언트는 쓸모가 없다.** `earth-limb.svg` 의 지구는 반지름 320인데 화면에 보이는 건 위쪽 80px뿐이어서, 기본 그라디언트로는 보이는 구간이 전체의 앞 10%에 몰려 단색으로 나왔다. `gradientUnits="userSpaceOnUse"` 로 좌표를 직접 잡아 보이는 구간에 색 변화를 몰아넣어 해결했다.
- **"먹을 것은 따뜻한 색"이라는 확정 규칙이 크리스탈형 쓰레기와 충돌했다.** 반짝이는 파편을 차가운 파랑으로 칠하면 규칙이 깨지고 위험물과의 대비도 흐려진다. 4종을 전부 따뜻한 색으로 묶고 **구분은 형태와 질감이 맡게** 했다. 색각 이상 사용자에게도 구분이 남는다는 부수 효과가 따라왔다.
- **색만 다른 파일 세 개는 토큰이 존재하는 이유와 어긋난다.** 배지 티어를 `badge-frame-bronze/silver/gold.svg` 로 나누려다, 프레임 하나에 `color` 를 티어 토큰으로 지정하는 방식으로 바꿨다.

측정한 수치: SVG 40개, 코드 없음이라 프레임률·번들 크기 측정은 M1로.

### 다음 할 일

- **M0 착수** — Next.js 14 App Router + TypeScript 스캐폴딩
  - 토큰은 새로 정하지 말고 `design/tokens/tokens.css` 를 `app/globals.css` 로 이식 (guide 2절)
  - 이식 후 두 파일이 어긋나지 않는지가 ADR-008의 감시 지점
- 워드마크의 한글이 `<text>` 라 기기 폰트를 탄다. 폰트를 정하면 패스로 변환해 교체할 것
- 배지 아트는 Q3(배지 종류와 획득 조건) 결론 후 M2에서

---

## 2026-07-25 — 문서 체계 정비 및 마일스톤 수립

- **마일스톤**: M0 착수 전 (사전 정비)
- **브랜치**: `claude/project-milestone-growth-docs-c1n44d`

### 한 일

- **CLAUDE.md가 존재하지 않는 구현을 서술하고 있던 문제를 바로잡았다.** 기존 문서는 "1차 프로토타입 범위: 아케이드 모드 + 프로필 대시보드까지 구현 완료"라고 적고 `app/page.tsx`, `app/play/page.tsx`, `app/profile/page.tsx`, `components/Jupsy.tsx`, `components/Joystick.tsx`, `lib/storage.ts` 6개 파일 목록을 코드 블록으로 제시했으나, **이 파일들은 어떤 브랜치·커밋에도 존재하지 않았다.** 레포에는 `CLAUDE.md` 한 파일과 커밋 하나(`d8b6789 init`)뿐이었다.
- 기획 내용(세계관·코어 루프·캐릭터 디자인·아케이드 규칙·보상 구조)은 전부 보존하고, 사실관계가 틀린 부분만 제거·교체했다.
- `docs/` 문서 체계를 만들었다 — 로드맵, ADR, 작업 로그, 성장 로드맵, 인덱스.
- 정해지지 않았던 기술 선택 세 가지를 확정했다: **CSS Modules + CSS 변수**(Tailwind 배제), **rAF + delta time 게임 루프**, **ESLint + Prettier + Vitest + GitHub Actions**.
- M0~M7 마일스톤을 정의하고, 결론이 필요한 질문 9개를 결론 시점과 함께 목록화했다.

### 검증

- `git log` — 문서 6개 외 변경 없음 확인
- CLAUDE.md에서 `app/`, `components/`, `lib/` 경로 grep — 남은 언급이 전부 "앞으로 만들 것"이라는 미래형 서술인지 확인
- 문서 간 상대 링크가 실제 파일명과 일치하는지 확인
- 마일스톤 ID(M0~M7)가 `roadmap.md`와 `growth-roadmap.md`에서 일관되게 쓰였는지 대조

### 배운 점

- **문서가 코드보다 앞서 나가면 문서가 아니라 오해가 된다.** 기존 CLAUDE.md는 "이렇게 만들 것"이라는 계획과 "이렇게 만들어져 있다"는 현황을 같은 문장 톤으로 섞어 썼고, 그 결과 다음에 읽는 사람이 없는 파일을 읽으려 하게 만들었다. 이번 정비에서 **계획과 현황을 문서 자체로 분리**했다 — 현황은 CLAUDE.md의 `현재 상태` 섹션 한 곳에만, 계획은 `docs/roadmap.md`에.
- 결정만 적고 근거를 안 적으면 나중에 뒤집을지 판단할 수 없다. ADR에 `근거`뿐 아니라 **`감시 지점`**(전제가 깨졌을 때 재검토할 조건)을 넣었다. 예: ADR-002는 SVG 렌더링의 오브젝트 수 상한이 게임 디자인을 감당 못 하면 재검토하도록, M1 실측값을 채울 빈칸을 남겨뒀다.
- 미결정 사항을 "미확정"이라고만 적어두면 영원히 미확정으로 남는다. 각 질문에 **결론이 나야 하는 마일스톤**을 붙였다.

측정한 수치는 없다 (코드 없음).

### 다음 할 일

- **M0 착수** — Next.js 14 App Router + TypeScript 스캐폴딩
  - `.gitignore`, `package.json`, `tsconfig.json`
  - CSS 변수 기반 디자인 토큰 (`app/globals.css`)
  - 모바일 세로 고정 레이아웃 셸 (safe-area 대응)
  - ESLint + Prettier + Vitest 설정
  - GitHub Actions CI (`lint → test → build`)
  - 홈 화면
- M0 완료 후 이 로그에 항목 추가, `docs/roadmap.md`의 "현재 위치" 표 갱신
