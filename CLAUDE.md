# 줍스 (Jupsy) — 반려위성 우주청소 게임

이 문서는 이 프로젝트의 **확정된 기획과 결정**을 담습니다. 이어서 작업할 때 아래 내용을 전제로 진행하면 됩니다. 사용자에게 다시 설명을 요구하지 말고, 여기 없는 부분만 새로 물어보세요.

각 섹션에는 `(확정)` / `(미확정)` 태그가 붙어 있습니다. **`(미확정)`은 아직 결정되지 않았다는 뜻이므로, 임의로 정하지 말고 물어보세요.**

## 현재 상태

> 이 섹션은 **실제 구현 상태**만 적습니다. 앞으로 만들 것은 여기가 아니라 [`docs/roadmap.md`](./docs/roadmap.md)에 있습니다.

- **애플리케이션 코드 없음.** 레포에는 문서(`CLAUDE.md`, `docs/`)만 존재합니다.
- `package.json`, `app/`, `components/`, `lib/` 모두 아직 만들어지지 않았습니다. **존재한다고 가정하고 읽으려 하지 마세요.**
- 진행 중인 마일스톤: **M0(프로젝트 기반) 착수 전**
- 다음 작업: Next.js 14 스캐폴딩 — 상세는 [`docs/roadmap.md`](./docs/roadmap.md)의 M0 참고

## 문서 맵

| 문서 | 역할 |
| --- | --- |
| `CLAUDE.md` (이 문서) | 컨셉·세계관·확정된 게임 규칙·기술 결정 요약·현재 상태 |
| [`docs/README.md`](./docs/README.md) | 문서 인덱스. 어떤 문서를 언제 갱신하는가 |
| [`docs/roadmap.md`](./docs/roadmap.md) | M0~M7 마일스톤. 범위·완료 기준·미결정 질문 목록 |
| [`docs/decisions.md`](./docs/decisions.md) | 기술 결정의 근거와 트레이드오프 (ADR) |
| [`docs/worklog.md`](./docs/worklog.md) | 작업 로그. 한 일·검증·배운 점·측정 수치 |
| [`docs/growth-roadmap.md`](./docs/growth-roadmap.md) | 마일스톤 연계 프론트엔드 학습 커리큘럼 |

**작업을 마치면 반드시 [`docs/worklog.md`](./docs/worklog.md)에 기록하세요.** 새 기술 선택을 했다면 [`docs/decisions.md`](./docs/decisions.md)에 ADR을 추가하고, 마일스톤을 완료했다면 위 `현재 상태` 섹션과 [`docs/roadmap.md`](./docs/roadmap.md)의 `현재 위치` 표를 갱신하세요.

## 한 줄 컨셉 (확정)

우주 쓰레기를 먹어치우는 반려위성을 입양해 지구에서 유대를 쌓고, 우주로 발사한 뒤에는 정기적으로 통신 연결되는 시간에 함께 청소 미션을 수행하는 게임. 캐릭터 이름은 **줍스**.

## 세계관 (확정)

- 가까운 미래, 화성 개발과 우주여행이 활성화되던 시기에 우려하던 **케슬러 신드롬**이 발생해 우주 쓰레기 문제가 심각해짐
- 국가 단위로 발사한 대형 청소위성은 너무 작게 흩어진 파편들 앞에서 효과가 미비했음
- 그래서 작고 많은 수의 반려동물형 위성 "줍스"를 개발함. 다만 수많은 줍스가 우주에서 지속가능하게 활동하려면 그만큼 사람의 조종과 애정(정서적 개입)이 필요했음
- 과학자들은 줍스에게 반려동물 같은 감성을 넣어 인류에게 분양하고, 사람들은 지구에서 줍스와 유대를 쌓은 뒤 발사체로 우주로 보냄
- 우주로 간 뒤에도 궤도를 도는 줍스와는 특정 시간(통신 가능 시간)에만 연결되어 함께 활동 가능
- **기획 의도**: 줍깅(플로깅)처럼 작은 실천이 환경을 살리는 자긍심으로 이어지는 경험을 게임으로 구현하고 싶음. 줍스를 분양받고 우주로 보내 청소한다는 행위 자체가 플레이어의 자긍심/자랑거리가 되도록 하는 것이 목표
- **바이럴 요소**: 줍스 입양~발사 과정을 사람과 사람 사이에 티켓/분양권 형태로 전달하며 퍼지는 구조를 원함 (구체 메커닉은 아직 미확정 — 선물형 분양권, 계보 시스템, 발사 이벤트 공유 등의 아이디어가 논의됐으나 확정 안 됨)

## 코어 루프 (확정)

두 개의 레이어로 구성:

1. **지구 레이어 (입양~발사, 방치형)**: 짧은 기간(당일~2~3일) 동안 먹이 주기/놀아주기로 유대를 쌓고 발사. 유대 형성 기간은 **짧게 가는 것으로 확정** — 첫날 임팩트를 압축적으로 주고, Day 1~2에 발사 준비 후 카운트다운 발사.
2. **우주 레이어 (통신 시간 = 아케이드 모드)**: 평소엔 다마고치/방치형처럼 존재하다가, 실제 위성처럼 하루 몇 번 정해진(그러나 매일 조금씩 달라지는) 통신 가능 시간에만 접속 가능. 이 짧은 창(패스 타임)에 접속하면 아케이드 모드로 전환해 실시간으로 쓰레기를 청소.

통신 가능 시간 설계는 **"진짜 위성처럼" 궤도 주기 기반의 정기 알림**으로 확정 (완전 랜덤이 아니라 매일 살짝 밀리는 방식). 협동/친구 연결 통신은 기본 루프가 아니라 가끔 발생하는 특별 이벤트로만 고려.

## 캐릭터 디자인 — 줍스 (확정, 단 성격 유형은 미확정)

- 로봇이지만 감정이 있는 존재 (월-E의 이브 같은 톤을 참고하되 동일 캐릭터는 아님)
- 디자인 방향은 다음 순서로 조정되어 확정됨: 처음엔 "기계적인 느낌 + 고슴도치/고양이 느낌(가시, 고양이 귀, 수염형 와이어)" → 이후 "각을 줄이고 귀여움을 극대화" → 최종적으로 **월-E 이브의 디자인 언어(이음새 없는 매끈한 흰 셸, 몸에서 분리되어 떠 있는 팔, 어두운 바이저 위에 발광하는 눈)를 참고한 고퀄리티 버전**으로 수렴
- 최종 형태 특징: 매끈한 흰색 달걀형 머리, 어두운 바이저 위에 하트 등으로 바뀌는 발광 눈, 몸통에서 분리되어 떠 있는 팔, 고양이 귀 형태의 짧은 센서, 등에 짧은 가시(고슴도치 요소), 수염 같은 와이어, 가슴에 기분/에너지를 나타내는 코어 라이트
- **표정 시스템**: 스탯창보다 표정/제스처/소리로 감정을 표현하는 방향. 좋아하는 걸 먹으면 눈이 하트 모양이 되고 통통 튐. 바이저를 하나의 디스플레이로 취급해 눈 모양만 바꾸면 감정 표현이 확장 가능하도록 설계 (개발 관점에서도 몸통 리깅 없이 눈 스프라이트 교체로 처리 가능)
- **성격 유형(구상 단계, 미확정 구현)**: 먹는 쓰레기 종류에 따라 성격/외형이 분화하는 4갈래 — 메탈형(금속 부품 선호, 씩씩함, 갑옷형 외형), 크리스탈형(반짝이는 파편 선호, 수다스러움, 크리스탈 장식), 아카이브형(오래된 필름/데이터 선호, 그리움 많음, 세피아톤), 케이블형(전선 선호, 장난기 많음, 케이블이 표정처럼 움직임). 이 유형별 실제 게임 반영 방식은 아직 상세 설계 전.

## 아케이드 모드 규칙 (확정)

- **조작**: 가상 조이스틱(자율 이동 + 방향 조작), 관성 있음
- **흡수**: 자동 흡수 — 부딪히면 그냥 먹음, 별도 액션 버튼 없음. 플레이어는 스틱 조작(이동/회피)에만 집중
- **성장**: 좋은 쓰레기를 먹을수록 크기가 커지고, 커질수록 더 큰 것도 먹을 수 있게 됨 (성장에 따른 판정 범위 확장)
- **위험 처리**: 위험 파편에 부딪히면 **크기/점수 감소만 되는 가벼운 패널티**, 게임 오버 없이 판이 계속 진행됨 (캐주얼하고 실패 부담 없는 톤 — 스트레스보다 성취/애정이 중심이라는 기획 의도와 일치)
- **시각 구분**: 먹을 것은 따뜻한 색/부드러운 형태, 위험한 것은 경고색/뾰족한 형태로 명확히 구분
- **세션 구조**: 패스 타임(2~5분, 프로토타입에서는 60초) 동안 진행, 시간 종료(신호 끊김 연출) 시 자동 종료 후 성과 요약

## 보상 구조 (확정)

**누적 자긍심/실적 중심**으로 결정. 재화로 전환해 상점에서 아이템을 사는 방식은 세계관의 진정성을 해칠 수 있어 배제하고, 성장과 실적 자체가 보상이 되는 방향을 선호함.

- **핵심 지표**: 누적 청소량(kg). 체감 가능한 비유로 환산해서 보여주는 것을 선호 ("자동차 3대 무게만큼 청소했어요" 등)
- **시각화**: 담당 궤도 구역이 점점 깨끗해지는(쓰레기 점이 사라지는) 형태의 진행 시각화
- **마일스톤/배지**: 재화가 아닌 명예/기록 형태의 배지 시스템 ("첫 1kg 청소" 등)
- 누적 실적이 다음 세대 줍스를 분양할 자격과 연결되는 아이디어도 논의됐으나 미확정 (바이럴 시스템과의 연결 지점)

## 기술 결정 (확정)

각 항목의 근거와 트레이드오프는 [`docs/decisions.md`](./docs/decisions.md)에 ADR로 기록되어 있습니다. 결정을 바꾸려면 그쪽을 먼저 읽으세요.

| 항목 | 결정 | ADR |
| --- | --- | --- |
| 프레임워크 | **Next.js 14 (App Router) + TypeScript**, Vercel 배포, 모바일 세로 화면 고정 | ADR-001 |
| 렌더링 | **SVG + CSS** (Canvas 2D 미사용) — 표정 교체를 선언적으로 다루기 위함 | ADR-002 |
| 스타일링 | **CSS Modules + CSS 변수** 기반 디자인 토큰 (Tailwind 미사용) | ADR-003 |
| 게임 루프 | **`requestAnimationFrame` + delta time** 정규화. 고빈도 값은 `useRef`, 저빈도 값만 `useState` | ADR-004 |
| 데이터 저장 | 브라우저 **`localStorage`** (스키마 버전 필드 포함). 서버/계정 저장은 M7로 유예 | ADR-005 |
| 패키지 매니저 | **npm** | — |
| 품질 도구 | **ESLint + Prettier + Vitest + GitHub Actions CI** (`lint → test → build`) | ADR-006 |
| 보상 설계 | **재화·상점 배제.** 누적 실적·배지만 | ADR-007 |

### 코드 규칙

- 색상·간격 값을 컴포넌트에 하드코딩하지 않고 CSS 변수를 거칩니다
- 게임 로직(성장 판정, 점수 계산, 배지 조건)은 순수 함수로 분리해 `lib/`에 두고 단위 테스트를 붙입니다
- `localStorage` 접근은 `lib/storage.ts` 한 곳으로만 합니다

## 미확정 항목

아래는 **아직 결정되지 않았습니다.** 임의로 정하지 말고 사용자에게 물어보세요. 각 항목이 어느 마일스톤에서 결론 나야 하는지는 [`docs/roadmap.md`](./docs/roadmap.md)의 미결정 질문 목록에 정리되어 있습니다.

- **바이럴 분양권/티켓 공유 시스템의 구체 메커닉** — 선물형 분양권, 계보 시스템, 발사 이벤트 공유 등이 논의됐으나 확정 안 됨
- **성격 유형 4종의 실제 게임 반영 방식** — 선호 쓰레기별 보너스, 외형 변화의 구체 설계
- **패스 타임 파라미터** — 하루 몇 회, 각 창의 길이, 매일 밀리는 폭
- **실제 세션 길이** — 프로토타입 60초를 최종 2~5분 중 어디로 할지
- **누적 실적과 다음 세대 분양 자격의 연결 여부** — 재화 배제 원칙(ADR-007)에 부합하는지 검토 필요
- **백엔드 선택** — 서버/계정 저장 도입 시

## 아직 만들지 않은 것

전부 미구현입니다 (`현재 상태` 참고). 순서와 완료 기준은 [`docs/roadmap.md`](./docs/roadmap.md)에 있습니다.

- 프로젝트 스캐폴딩 자체 (M0)
- 아케이드 모드 (M1)
- 누적 실적 저장과 프로필 대시보드 (M2)
- 캐릭터 SVG 고퀄리티 버전 — 분리된 부유형 팔, 바이저 발광 눈 표정 세트 (M3)
- 입양~발사 온보딩 플로우 (M4)
- 통신 가능 시간 스케줄링과 알림 (M5)
- 성격 유형 분화 (M6)
- 서버/계정 저장, 바이럴 공유 (M7)
